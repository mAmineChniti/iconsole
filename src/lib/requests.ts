/*import { TFetchClient } from "@thatguyjamal/type-fetch";
import { type LoginRequest } from "@/types/LoginInterface";

const tfetch = new TFetchClient();
import { env } from "@/env";
const NEXT_PUBLIC_BACKEND = env.NEXT_PUBLIC_BACKEND;
const API_CONFIG = {
  AUTH: {
    BASE_URL: NEXT_PUBLIC_BACKEND ?? "http://localhost:8000/api/auth",
    ENDPOINTS: {
      LOGIN: "/login",
    },
  },
} as const;*/
